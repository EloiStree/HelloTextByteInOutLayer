If you received a `MANUAL|text` Tag.
It means that the received `String` is the manual of how to use the game.

Depending on the game and the tournament, it can be sent every n seconds or called with an input.
