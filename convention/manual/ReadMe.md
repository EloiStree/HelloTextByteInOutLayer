If you receive a `MANUAL|text` tag, it means the accompanying `String` contains the game's instruction manual.

Depending on the game or tournament configuration, this manual may be sent automatically at regular intervals (every *n* seconds) or returned in response to a specific input.

To request the manual manually, send `MANUAL` through the text input.
